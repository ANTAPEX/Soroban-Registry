## Unreleased

### Added

- Backend: Swagger/OpenAPI documentation (`/api/docs`, `/api/v1/openapi.json`, `/api/v2/openapi.json`).
- Backend: URL-based API versioning (`/api/v1`, `/api/v2`) with deprecated legacy alias (`/api/...`) and deprecation headers.
- Backend: Webhook system for contract events with retries, HMAC signatures, delivery history and dead letter queue.
- CLI: `log` command for viewing, tailing, filtering and exporting CLI logs.

